import React, {useState} from 'react'
import menu from './data'


const Item = ({id, category, title, price, desc}) => {
    const [item, setItem] = useState({})

    return (
        <article>Item</article>
    )
}

export default Item